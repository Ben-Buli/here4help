# 任務爭議(Dispute)模組實現規格文件

## 📋 **專案架構分析總結**


### 🔍 **現有架構評估**

#### **1. Flutter App 端現有功能**
✅ **可直接使用的功能**:
- `DisputeDialog` 已實現 (lib/widgets/dispute_dialog.dart)
- `DisputeApi` 已實現 (lib/services/api/dispute_api.dart)
- Action Bar 中的 'dispute' 按鈕已配置
- `_handleDispute()` 方法已實現
- 表單驗證、字數統計功能已具備

❌ **需要修改的功能**:
- DisputeDialog 需要重新設計以符合新需求規格
- DisputeApi 需要對接新的 task_dispute_events 資料結構
- 需要新增重複申請檢查邏輯
- 需要新增倒數計時處理邏輯

#### **2. Admin Web 端現有功能**
✅ **可重用的架構**:
- TasksView.vue 已存在，可擴展爭議功能
- 現有的 taskApi 服務架構
- 現有的路由和權限系統
- 現有的表格和篩選組件

❌ **需要新增的功能**:
- 專門的任務爭議列表頁面
- 爭議案件詳情頁面
- 管理員決策 Dialog 組件
- 爭議相關的 API 服務

#### **3. Backend API 現有功能**
✅ **可參考的實現**:
- tasks/dispute.php 已存在但使用舊資料結構
- 現有的 JWT 認證機制
- 現有的 Response 工具類
- 現有的資料庫操作模式

❌ **需要重新實現**:
- 完全重寫 dispute API 以使用新的 task_dispute_events 結構
- 新增管理員爭議處理 API
- 新增爭議列表查詢 API

## 🎯 **實現規格詳細說明**

### **一、Flutter App 端實現**

#### **1.1 DisputeDialog 重構**
**檔案**: `lib/widgets/dispute_dialog.dart`

**需要修改的內容**:
```dart
class DisputeDialog extends StatefulWidget {
  final String taskId;
  final String chatRoomId; // 新增
  final String taskTitle;
  final VoidCallback? onDisputeSubmitted;
}

class _DisputeDialogState extends State<DisputeDialog> {
  final _titleController = TextEditingController(); // 新增
  final _descriptionController = TextEditingController();
  
  int _titleCharCount = 0; // 新增字數統計
  int _descriptionCharCount = 0; // 新增字數統計
  
  // 預先檢查是否已存在爭議
  Map<String, dynamic>? _existingDispute;
  bool _isLoading = true;
  
  @override
  void initState() {
    super.initState();
    _checkExistingDispute();
  }
  
  Future<void> _checkExistingDispute() async {
    try {
      final result = await DisputeApi.checkExistingDispute(
        chatRoomId: widget.chatRoomId,
      );
      
      setState(() {
        _existingDispute = result['dispute'];
        _isLoading = false;
      });
    } catch (e) {
      setState(() {
        _isLoading = false;
      });
    }
  }
}
```

**新增功能**:
- 只顯示 title 和 description 欄位
- 字數統計 (0/255 for title, 0/1000 for description)
- 預先檢查重複申請
- 錯誤處理和紅字顯示
- **防重複提交機制**: 如果已存在爭議，顯示現有爭議資訊而非提交表單

**UI 邏輯**:
```dart
Widget build(BuildContext context) {
  if (_isLoading) {
    return Center(child: CircularProgressIndicator());
  }
  
  // 如果已存在爭議，顯示現有爭議資訊
  if (_existingDispute != null) {
    return AlertDialog(
      title: Text('Dispute Already Exists'),
      content: Column(
        mainAxisSize: MainAxisSize.min,
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text('A dispute has already been submitted for this task.'),
          SizedBox(height: 16),
          Text('Dispute ID: ${_existingDispute!['id']}'),
          Text('Status: ${_existingDispute!['status']}'),
          Text('Submitted: ${_formatDateTime(_existingDispute!['created_at'])}'),
        ],
      ),
      actions: [
        TextButton(
          onPressed: () => Navigator.of(context).pop(),
          child: Text('OK'),
        ),
      ],
    );
  }
  
  // 如果沒有現有爭議，顯示提交表單
  return AlertDialog(
    title: Text('Submit Dispute'),
    content: // ... 原有的表單內容
  );
}
```

#### **1.2 DisputeApi 重構**
**檔案**: `lib/services/api/dispute_api.dart`

**沿用現有架構**:
- ✅ 使用 `AppConfig.api()` 構建 API URL
- ✅ 使用 `HttpClientService` 進行 HTTP 請求
- ✅ 統一的錯誤處理和 JSON 解析模式

**需要修改的 API 端點**:
```dart
class DisputeApi {
  // 檢查是否已存在爭議
  static Future<Map<String, dynamic>> checkExistingDispute({
    required String chatRoomId,
  }) async {
    final response = await HttpClientService.get(
      AppConfig.api('/task-disputes/check.php?chat_room_id=$chatRoomId')
    );
    
    if (response.statusCode == 200) {
      final data = json.decode(response.body);
      return {
        'exists': data['data']['exists'] ?? false,
        'dispute': data['data']['dispute'], // 包含 id, status, created_at 等資訊
      };
    }
    
    throw Exception('Failed to check existing dispute');
  }
  
  // 提交爭議 (重構)
  static Future<Map<String, dynamic>> submitDispute({
    required String taskId,
    required String chatRoomId,
    required String title,
    required String description,
  }) async {
    final response = await HttpClientService.post(
      AppConfig.api('/task-disputes/create.php'),
      body: {
        'task_id': taskId,
        'task_dispute_chat_room_id': chatRoomId,
        'title': title,
        'description': description,
      },
    );
    // 沿用現有的回應處理模式
  }
  
  // 獲取爭議列表 (用於 /issue-status 頁面)
  static Future<List<Map<String, dynamic>>> getDisputeList() async {
    final response = await HttpClientService.get(
      AppConfig.api('/task-disputes/list.php')
    );
    // 沿用現有的列表解析模式
  }
}
```

**新增 AppConfig 端點**:
```dart
// 在 lib/config/app_config.dart 中新增
static String get taskDisputeCheckUrl => api('/task-disputes/check.php');
static String get taskDisputeCreateUrl => api('/task-disputes/create.php');
static String get taskDisputeListUrl => api('/task-disputes/list.php');
```

#### **1.3 Issue Status 頁面**
**檔案**: 需要新建或修改現有頁面

**功能要求**:
- 使用 tabs 呈現 (Support Events + Dispute Events)
- 只顯示 status in ('submitted', 'in_progress') 的案件
- 顯示案件編號、時間線、當前狀態

### **二、Admin Web 端實現**

#### **2.1 任務爭議列表頁面**
**檔案**: `admin/frontend/src/views/TaskDisputesView.vue` (新建)

**頁面結構**:
```vue
<template>
  <div class="space-y-6">
    <!-- 頁面標題 -->
    <div class="md:flex md:items-center md:justify-between">
      <h2 class="text-2xl font-bold">Task Disputes Management</h2>
    </div>
    
    <!-- 篩選器 -->
    <div class="admin-card">
      <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">
        <!-- 狀態篩選、日期範圍等 -->
      </div>
    </div>
    
    <!-- 爭議列表表格 -->
    <div class="admin-card">
      <table class="min-w-full divide-y divide-gray-200">
        <thead>
          <tr>
            <th>Dispute ID</th>
            <th>Task Title</th>
            <th>Submitter</th>
            <th>Status</th>
            <th>Created At</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <!-- 爭議列表 -->
        </tbody>
      </table>
    </div>
  </div>
</template>
```

**Action 按鈕**:
- `Room` - 進入聊天室（唯讀模式）
- `Operation` - 管理員決策

#### **2.1.1 管理員聊天室查看頁面**
**檔案**: `admin/frontend/src/views/AdminChatRoomView.vue` (新建)

**功能特點**:
- **唯讀模式**: 只能查看訊息，無法發送訊息或上傳圖片
- **完整訊息歷史**: 顯示爭議任務的完整聊天記錄
- **用戶身份標識**: 清楚標示每則訊息的發送者（Creator/Tasker）
- **多種訊息類型支援**: 文字、圖片、系統訊息、Resume等
- **時間軸顯示**: 按時間順序顯示所有訊息

**頁面結構**:
```vue
<template>
  <div class="admin-chat-room-view">
    <!-- 頁面標題區 -->
    <div class="chat-header bg-gray-50 p-4 border-b">
      <div class="flex items-center justify-between">
        <div>
          <h2 class="text-xl font-bold text-gray-900">Dispute Chat Room</h2>
          <div class="mt-1 text-sm text-gray-600">
            <p>Task: {{ taskTitle }}</p>
            <p>Dispute ID: {{ disputeId }} | Status: {{ disputeStatus }}</p>
          </div>
        </div>
        <button @click="goBack" class="btn-secondary">
          <Icon name="arrow-left" class="mr-2" />
          Back to Disputes
        </button>
      </div>
    </div>

    <!-- 聊天訊息區域 -->
    <div class="chat-messages-container flex-1 overflow-y-auto p-4">
      <div class="messages-list space-y-4">
        <AdminMessageBubble 
          v-for="message in messages" 
          :key="message.id"
          :message="message"
          :users="usersMap"
          :task="taskInfo"
        />
      </div>
    </div>

    <!-- 底部資訊欄（替代輸入區） -->
    <div class="chat-footer-info border-t bg-gray-50">
      <div class="flex items-center justify-center py-3">
        <Icon name="eye" class="text-gray-400 mr-2" />
        <span class="text-sm text-gray-600">Admin Read-Only Mode - Total {{ messages.length }} messages</span>
      </div>
    </div>
  </div>
</template>
```

**組件設計**:
- `AdminMessageBubble`: 管理員專用訊息氣泡組件
- `MessageContentRenderer`: 多類型訊息內容渲染器
- `UserRoleBadge`: 用戶角色標識組件

**資料結構**:
```typescript
interface AdminChatRoomData {
  chat_room: {
    id: string;
    type: 'application';
    task_id: string;
  };
  task: {
    id: string;
    title: string;
    creator_id: number;
    participant_id: number;
  };
  messages: ChatMessage[];
  users: Record<number, UserInfo>;
  dispute_info: {
    id: number;
    title: string;
    status: string;
    created_at: string;
  };
}
```

#### **2.2 管理員決策 Dialog**
**檔案**: `admin/frontend/src/components/DisputeOperationDialog.vue` (新建)

**Dialog 結構**:
```vue
<template>
  <div class="fixed inset-0 z-50 overflow-y-auto">
    <div class="modal-content">
      <h3>Dispute Resolution</h3>
      
      <!-- 爭議資訊顯示 -->
      <div class="dispute-info">
        <!-- 任務資訊、爭議原因等 -->
      </div>
      
      <!-- 決策選項 -->
      <div class="decision-options">
        <select v-model="selectedDecision">
          <option value="completed">Mark Task as Completed</option>
          <option value="back_to_progress">Back to In Progress</option>
          <option value="reset">Reset Task</option>
        </select>
      </div>
      
      <!-- 決策說明 -->
      <textarea v-model="decisionNote" placeholder="Decision explanation..."></textarea>
      
      <!-- 操作按鈕 -->
      <div class="modal-actions">
        <button @click="submitDecision">Submit Decision</button>
        <button @click="closeDialog">Cancel</button>
      </div>
    </div>
  </div>
</template>
```

#### **2.3 路由配置**
**檔案**: `admin/frontend/src/router/index.ts`

**新增路由**:
```typescript
{
  path: '/task-disputes',
  component: () => import('../components/AppLayout.vue'),
  meta: { requiresAuth: true, permission: 'disputes.list' },
  children: [
    {
      path: '',
      name: 'task-disputes',
      component: () => import('../views/TaskDisputesView.vue'),
      meta: { title: 'Task Disputes' },
    },
    {
      path: ':id',
      name: 'dispute-detail',
      component: () => import('../views/DisputeDetailView.vue'),
      meta: { title: 'Dispute Detail' },
    },
    {
      path: ':disputeId/chat-room',
      name: 'admin-dispute-chat-room',
      component: () => import('../views/AdminChatRoomView.vue'),
      meta: { 
        title: 'Dispute Chat Room',
        permission: 'disputes.view'
      },
    },
  ],
}
```

#### **2.4 API 服務**
**檔案**: `admin/frontend/src/services/api.ts`

**新增 API**:
```typescript
export const disputeApi = {
  list: (params?: {
    page?: number
    per_page?: number
    status?: string
    date_from?: string
    date_to?: string
    sort_by?: string
    sort_order?: 'asc' | 'desc'
  }) => api.get<PaginatedResponse<any>>('/api/admin/task-disputes', { params }),

  show: (id: string) => api.get<ApiResponse<any>>(`/api/admin/task-disputes/${id}`),

  resolve: (id: string, decision: string, note: string) =>
    api.patch<ApiResponse>(`/api/admin/task-disputes/${id}/resolve`, { decision, note }),

  // 新增：管理員查看聊天室
  getChatRoom: (disputeId: string) => 
    api.get<ApiResponse<AdminChatRoomData>>(`/api/admin/task-disputes/${disputeId}/chat-room`),
}
```

### **三、Backend API 實現**

#### **3.1 任務爭議建立 API**
**檔案**: `backend/api/task-disputes/create.php` (新建)

**沿用現有架構**:
- ✅ 使用 `JWTManager::validateRequest()` 進行 JWT 認證
- ✅ 使用 `Response::success()` 和 `Response::error()` 統一回應格式
- ✅ 使用 `Database::getInstance()` 獲取資料庫連線
- ✅ 使用 `UserActiveLogger::logAction()` 記錄使用者操作
- ✅ 使用資料庫交易 (`$db->beginTransaction()`, `$db->commit()`, `$db->rollback()`)

**功能**:
- JWT 認證和權限檢查
- 檢查重複申請
- 驗證任務狀態 (只允許 status_id in [2,3])
- 建立 task_dispute_events 記錄
- 更新 tasks.status_id = 4 (dispute)
- 記錄 task_dispute_event_logs
- 使用 UserActiveLogger 記錄操作

**API 結構**:
```php
<?php
// POST /api/task-disputes/create.php
require_once __DIR__ . '/../../config/database.php';
require_once __DIR__ . '/../../utils/JWTManager.php';
require_once __DIR__ . '/../../utils/Response.php';
require_once __DIR__ . '/../../utils/UserActiveLogger.php';

// 沿用現有的 JWT 認證模式
$tokenData = JWTManager::validateRequest();
if (!$tokenData['valid']) {
    Response::unauthorized($tokenData['message']);
}

// 沿用現有的資料庫交易模式
$db = Database::getInstance()->getConnection();
$db->beginTransaction();

try {
    // 業務邏輯處理
    // ...
    
    // 使用 UserActiveLogger 記錄操作
    UserActiveLogger::logAction(
        $db, $userId, 'task_dispute_created',
        'task_status', $oldStatus, 'dispute',
        "Task dispute created: $title",
        'user', $userId, null, null,
        ['task_id' => $taskId, 'dispute_id' => $disputeId]
    );
    
    $db->commit();
    Response::success(['dispute_id' => $disputeId], 'Dispute created successfully');
} catch (Exception $e) {
    $db->rollback();
    Response::serverError('Failed to create dispute: ' . $e->getMessage());
}
```

#### **3.2 重複檢查 API**
**檔案**: `backend/api/task-disputes/check.php` (新建)

**沿用現有架構**:
- ✅ 使用 `JWTManager::validateRequest()` 進行 JWT 認證
- ✅ 使用 `Response::success()` 統一回應格式

**功能**:
- 檢查指定 chat_room_id 是否已存在爭議記錄
- 如果存在，返回現有爭議的詳細資訊 (id, status, created_at)
- 如果不存在，返回 exists: false

**API 輸入**:
```
GET /api/task-disputes/check.php?chat_room_id={chat_room_id}
Authorization: Bearer {jwt_token}
```

**API 輸出**:
```json
// 不存在爭議時
{
  "success": true,
  "data": {
    "exists": false,
    "dispute": null
  }
}

// 存在爭議時
{
  "success": true,
  "data": {
    "exists": true,
    "dispute": {
      "id": 123,
      "title": "Payment issue",
      "status": "submitted",
      "created_at": "2024-01-16T09:00:00Z"
    }
  }
}
```

#### **3.3 管理員爭議列表 API**
**檔案**: `backend/api/admin/task-disputes.php` (新建)

**沿用現有架構**:
- ✅ 使用 `JWTManager::validateRequest()` 進行管理員 JWT 認證
- ✅ 使用 `Pagination` 工具類處理分頁
- ✅ 使用 `Response::success()` 統一回應格式
- ✅ 參考現有 `backend/api/admin/tasks.php` 的實現模式

**功能**:
- 管理員權限檢查 (`admin_roles.name IN ('admin', 'super_admin')`)
- 分頁查詢 task_dispute_events
- 支援篩選和排序
- 關聯任務和使用者資訊

#### **3.4 管理員聊天室查看 API**
**檔案**: `backend/api/admin/task-disputes/chat-room.php` (新建)

**沿用現有架構**:
- ✅ 使用 `JWTManager::validateRequest()` 進行管理員 JWT 認證
- ✅ 使用 `Response::success()` 統一回應格式
- ✅ 參考 `backend/api/chat/get_messages.php` 的訊息查詢邏輯

**功能**:
- 管理員權限檢查 (`admin_roles.name IN ('admin', 'super_admin')`)
- 根據 dispute_id 查詢對應的聊天室
- 返回完整聊天記錄和用戶資訊
- 支援多種訊息類型的完整資料

**API 輸入**:
```
GET /api/admin/task-disputes/{dispute_id}/chat-room
Authorization: Bearer {admin_jwt_token}
```

**API 輸出**:
```json
{
  "success": true,
  "data": {
    "chat_room": {
      "id": "room_123",
      "type": "application",
      "task_id": "task_456"
    },
    "task": {
      "id": "task_456",
      "title": "Fix website bug",
      "creator_id": 10,
      "participant_id": 20,
      "reward_point": 500,
      "status": {
        "code": "dispute",
        "display_name": "Dispute"
      }
    },
    "messages": [
      {
        "id": 1001,
        "room_id": "room_123",
        "from_user_id": 10,
        "content": "Hello, I need help with...",
        "kind": "text",
        "created_at": "2024-01-15T10:30:00Z"
      },
      {
        "id": 1002,
        "room_id": "room_123", 
        "from_user_id": 20,
        "content": "Application submitted with resume data...",
        "kind": "resume",
        "created_at": "2024-01-15T11:00:00Z"
      }
    ],
    "users": {
      "10": {
        "id": 10,
        "name": "John Creator",
        "avatar_url": "backend/uploads/avatars/john.jpg"
      },
      "20": {
        "id": 20,
        "name": "Jane Tasker", 
        "avatar_url": "backend/uploads/avatars/jane.jpg"
      }
    },
    "dispute_info": {
      "id": 5,
      "title": "Payment issue",
      "status": "submitted",
      "created_at": "2024-01-16T09:00:00Z"
    }
  }
}
```

**處理邏輯**:
1. **權限檢查**: 驗證管理員角色和爭議查看權限
2. **資料查詢**: 
   - 從 `task_dispute_events` 查詢爭議資訊
   - 從 `chat_rooms` 查詢對應聊天室
   - 從 `chat_messages` 查詢完整訊息記錄
   - 從 `users` 查詢參與用戶資訊
3. **資料整合**: 組合所有相關資料並返回

#### **3.5 管理員決策 API**
**檔案**: `backend/api/admin/task-disputes/resolve.php` (新建)

**功能**:
- 更新 task_dispute_events 狀態和決策
- 根據決策更新 tasks 狀態
- 記錄 task_dispute_event_logs
- 處理點數轉移和手續費
- 發送系統訊息到聊天室

**API 輸入**:
```json
{
  "decision_result": "completed|back_to_progress|reset",
  "decision_note": "管理員決策說明"
}
```

**沿用現有架構**:
- ✅ 使用 `JWTManager::validateRequest()` 進行管理員 JWT 認證
- ✅ 使用資料庫交易確保資料一致性
- ✅ 使用 `UserActiveLogger::logAction()` 記錄管理員操作
- ✅ **完全重用** `backend/api/tasks/confirm_completion.php` 的點數轉移邏輯
- ✅ 參考現有 `backend/api/chat/send_message.php` 的系統訊息發送模式

**現有點數轉移邏輯分析**:
根據 `confirm_completion.php` 的實現，包含以下完整流程：
1. **手續費計算**: 從 `task_completion_points_fee_settings` 讀取費率
2. **點數轉移**: 
   - 創建者支出任務獎勵 (`PointTransactionLogger::logTaskSpending`)
   - 接案者收入淨額 (`PointTransactionLogger::logTaskEarning`)
   - 創建者支出手續費 (`PointTransactionLogger::logFee`)
3. **手續費記錄**: 寫入 `fee_revenue_ledger`
4. **用戶餘額更新**: 直接更新 `users.points`
5. **活動日誌**: 寫入 `user_active_log`
6. **系統訊息**: 發送到聊天室
7. **任務狀態更新**: 更新 `tasks.status_id` 和 `task_applications.status`

**處理邏輯**:
1. **權限檢查**: 驗證管理員角色 ('admin', 'super_admin')
2. **狀態更新**: 
   - `task_dispute_events.status = 'resolved'`
   - `task_dispute_events.decision_result = {decision}`
   - `task_dispute_events.decision_note = {note}`
   - `task_dispute_events.admin_id = {admin_id}`
3. **任務狀態處理**: 根據決策選項執行相應邏輯
4. **點數處理**: **completed 決策時直接調用 confirm_completion.php 的邏輯**
5. **日誌記錄**: 
   - 寫入 `task_dispute_event_logs`
   - 使用 `UserActiveLogger` 記錄管理員操作
6. **系統訊息**: 發送到爭議聊天室，格式如下：
   ```
   Admin Resolution Result: {decision_result}
   Description: {decision_note}
   
   Issue Status: resolved
   Last Update Time: {updated_at}
   ```

### **四、資料庫變更需求**

#### **4.1 確認資料表結構**
根據 SQL 檔案，以下資料表已存在：
- ✅ `task_dispute_events`
- ✅ `task_dispute_event_logs`

#### **4.2 任務狀態ID對應關係**
根據 `backend/database/migrations/2025_08_08_000001_create_task_statuses.sql`：

| ID | Code | Display Name | 說明 |
|----|------|--------------|------|
| 1 | open | Open | 開放應徵 |
| 2 | in_progress | In Progress | 進行中 |
| 3 | pending_confirmation | Pending Confirmation | 等待確認 |
| 4 | dispute | Dispute | 爭議中 |
| 5 | completed | Completed | 已完成 |
| 6 | applying | Applying | 應徵中 |
| 7 | rejected | Rejected | 已拒絕 |

#### **4.3 需要確認的欄位（已確認）**
- ✅ `task_dispute_events.task_dispute_chat_room_id` 關聯到 `chat_rooms.id`
- ✅ `task_statuses` 中 `code='dispute'` 對應 `id=4`
- ✅ 所有狀態ID已正確定義

## 🚨 **需要決策的問題（已決策）**

### **1. 倒數計時處理**
**問題**: `tasks.status_id = 3` (pending confirmation) 的7*24小時倒數計時功能的處理方式？

**現有倒數計時實現位置**:
- **檔案**: `backend/cron/auto_complete_tasks.php`
- **功能**: 檢查 pending confirmation 狀態超過7天的任務，自動標記為 completed
- **執行方式**: 透過 cron job 定期執行（建議每小時執行一次）
- **判斷邏輯**: `DATEDIFF(NOW(), t.updated_at) >= 7` 基於 `tasks.updated_at` 時間戳

**決策**:
- 採用自動取消倒數計時做法
- 當任務狀態變更為 dispute (status_id = 4) 時，由於不再是 pending_confirmation 狀態，cron 腳本會自動跳過該任務
- 無需額外處理，現有機制已能正確處理


### **2. 資料表關聯**
**問題**: `task_dispute_events.task_dispute_chat_room_id` 的關聯方式？
**決策**:
- 直接關聯到 `chat_rooms.id` 也就是當dispute dialog使用者在填寫的時候，會將該 /chat/detail對應的 room id帶入一併送到資料表


### **3. 管理員權限**
**問題**: 哪些管理員可以處理爭議？
**決策**:
- 特定權限的管理員(依照資料表 `admins.role_id` = `admin_roles.id` 列出有權限的管理員角色有權限處理爭議，先預設'admin', 'super_admin')


### **4. 爭議狀態流程**
**問題**: 爭議解決後的狀態流程？

**決策**: 直接更新任務狀態，依照管理員決策進行不同處理

#### **管理員決策選項** (`task_dispute_events.decision_result`)
根據資料表定義：`enum('completed', 'back_to_progress', 'reset')`

**1. `completed` - 判定任務直接完成**
- **任務狀態**: `tasks.status_id = 5` (completed)
- **任務執行者應徵紀錄**: 
  - 查詢條件: `tasks.participant_id = task_applications.user_id`
  - 更新: `task_applications.status = 'completed'`
- **點數轉移**: **完全重用 `confirm_completion.php` 邏輯**
  - 創建者支出任務獎勵點數
  - 接案者收入淨額（扣除手續費）
  - 創建者支出手續費
  - 更新 `users.points` 餘額
  - 記錄到 `point_transactions` 和 `fee_revenue_ledger`
  - 寫入 `user_active_log`

**2. `back_to_progress` - 判定任務回歸進行中**
- **任務狀態**: `tasks.status_id = 2` (in_progress)
- **任務執行者應徵紀錄**: 
  - 查詢條件: `tasks.participant_id = task_applications.user_id`
  - 更新: `task_applications.status = 'accepted'` (對應 in_progress 狀態)

**3. `reset` - 重置任務並取消現有執行者**
- **任務狀態**: `tasks.status_id = 1` (open)
- **任務執行者**: `tasks.participant_id = NULL`
- **原執行者應徵紀錄**: `task_applications.status = 'rejected'`
- **其他應徵者**: 保持原狀態，可重新競爭
- **限制**: 原執行者無法再次應徵此任務

#### **系統訊息通知機制**
所有判決完成後，系統會自動發送判決結果到聊天室：

**發送邏輯**:
1. **目標聊天室**: `task_dispute_events.task_dispute_chat_room_id` 對應的 `chat_rooms.id`
2. **訊息類型**: `chat_messages.kind = 'system'`
3. **訊息內容**: 
   ```
   Decision: {decision_result}
   Note: {decision_note}
   ```
4. **發送者**: 系統 (from_user_id = 1 或管理員ID)

#### **操作日誌記錄**
所有管理員操作都需記錄在 `task_dispute_event_logs` 資料表：
- **事件ID**: `event_id` (關聯 task_dispute_events.id)
- **操作管理員**: `admin_id`
- **狀態變更**: `old_status` → `new_status`
- **決策變更**: `old_decision` → `new_decision`
- **操作說明**: `note`


## 📊 **實現優先級**

### **高優先級 (必須實現)**
1. ✅ **DisputeDialog 重構** (含防重複提交機制)
   - 預先檢查現有爭議
   - 顯示現有爭議資訊而非重複提交表單
   - 包含 ID、狀態、時間戳記顯示
2. ✅ **Backend 爭議建立 API**
   - task-disputes/create.php
   - task-disputes/check.php (返回現有爭議詳細資訊)
3. ✅ **Admin 爭議列表頁面**
4. ✅ **管理員決策功能**
5. ✅ **管理員聊天室查看功能**
   - AdminChatRoomView.vue 頁面
   - AdminMessageBubble 組件
   - backend/api/admin/task-disputes/chat-room.php API
   - 管理員操作記錄 (admin_active_logs)

### **中優先級 (建議實現)**
1. 🔄 Issue Status 頁面整合
2. 🔄 即時通知功能
3. 🔄 爭議統計功能

### **低優先級 (可延後：暫時不做)**
1. ⏳ 爭議歷史查詢(管理員後台端、APP端)
2. ⏳ 批量處理功能
3. ⏳ 匯出功能

## 🛠 **開發步驟建議**

### **階段一：Backend 基礎**
1. 建立 task-disputes API 端點
2. 實現資料驗證和業務邏輯
3. 測試 API 功能

### **階段二：Flutter App**
1. 重構 DisputeDialog
2. 更新 DisputeApi
3. 整合 Issue Status 頁面

### **階段三：Admin Web**
1. 建立爭議列表頁面
2. 實現管理員決策功能
3. 整合路由和權限

### **階段四：測試與優化**
1. 端到端測試
2. 效能優化
3. 使用者體驗改善

## 🔧 **現有架構可重用性評估**

### **✅ 高度可重用的現有功能**

#### **1. 認證與授權**
- **JWT 認證**: `JWTManager::validateRequest()` - 完全可重用
- **權限檢查**: 現有的管理員角色驗證機制
- **Token 處理**: 支援 Header 和 URL 參數多種方式

#### **2. 資料庫操作**
- **連線管理**: `Database::getInstance()` - 完全可重用
- **交易處理**: `beginTransaction()`, `commit()`, `rollback()` - 完全可重用
- **分頁工具**: `Pagination` 類 - 可直接用於爭議列表

#### **3. API 回應格式**
- **統一回應**: `Response::success()`, `Response::error()` - 完全可重用
- **錯誤處理**: `ErrorCodes` 和統一的錯誤碼系統
- **CORS 處理**: `Response::setCorsHeaders()` - 完全可重用

#### **4. 日誌記錄**
- **使用者操作**: `UserActiveLogger::logAction()` - 完全可重用
- **系統日誌**: `Logger` 類的各種日誌方法
- **追蹤ID**: `TraceId` 用於請求追蹤

#### **5. Frontend 架構**
- **API 配置**: `AppConfig.api()` - 完全可重用
- **HTTP 客戶端**: `HttpClientService` - 完全可重用
- **錯誤處理**: 現有的 try-catch 和 SnackBar 模式

### **🔄 需要適配的現有功能**

#### **1. 點數轉移邏輯**
- **現有**: `backend/api/tasks/confirm_completion.php`
- **適配**: 爭議決策為 'completed' 時調用相同邏輯
- **優勢**: 無需重寫複雜的點數和手續費計算

#### **2. 系統訊息發送**
- **現有**: `backend/api/chat/send_message.php` 的系統訊息模式
- **適配**: 爭議決策結果的系統訊息發送
- **優勢**: 沿用現有的聊天室訊息格式

#### **3. Admin 列表頁面**
- **現有**: `admin/frontend/src/views/TasksView.vue`
- **適配**: 複製結構建立 `TaskDisputesView.vue`
- **優勢**: UI 組件和篩選邏輯可直接重用

### **📊 重用性統計**

| 功能類別 | 可重用程度 | 說明 |
|---------|-----------|------|
| **認證授權** | 100% | JWT、權限檢查完全可重用 |
| **資料庫操作** | 100% | 連線、交易、分頁完全可重用 |
| **API 回應** | 100% | Response 類完全可重用 |
| **日誌記錄** | 100% | UserActiveLogger 完全可重用 |
| **Frontend HTTP** | 100% | AppConfig、HttpClientService 完全可重用 |
| **業務邏輯** | 80% | 點數轉移、系統訊息需要適配 |
| **UI 組件** | 90% | Admin 表格、篩選組件可重用 |

### **💡 實現建議**

1. **優先重用**: 先使用現有架構，確保一致性
2. **參考實現**: 複製類似功能的代碼結構
3. **統一標準**: 遵循現有的命名和格式約定
4. **漸進開發**: 先實現基本功能，再添加特殊邏輯

## ⚠️ **重要實現注意事項**

### **1. 資料一致性**
- 爭議建立時必須檢查任務當前狀態，只允許特定狀態(task.status_id in 2:in progress, 3: pending confirmation )下建立爭議
- 管理員決策時需要再次驗證任務狀態，避免併發問題
- 所有狀態變更都需要使用資料庫交易確保一致性

### **2. 權限控制**
- 只有任務相關人員 (tasks.creator_id 或 tasks.participant_id) 可以建立爭議
- 管理員權限檢查：`admins.role_id` 對應 `admin_roles.name IN ('admin', 'super_admin')`
- API 端點需要適當的 JWT 認證和權限驗證

### **3. 業務邏輯驗證**
- 防止重複建立爭議：檢查 `task_dispute_chat_room_id` 是否已存在
- 倒數計時自動處理：狀態變更為 dispute 後，cron 腳本會自動跳過
- 點數轉移和手續費：completed 決策需要調用現有的轉點 API

### **4. 使用者體驗**
- 爭議建立後立即更新 UI 狀態
- 系統訊息需要清晰易懂的格式
- 錯誤訊息需要提供具體的解決建議

## 📝 **總結**

現有專案已具備基礎架構，但需要針對新的資料結構進行重構。主要工作量集中在：

1. **Backend API 重寫** (約 60% 工作量)
2. **Frontend 組件調整** (約 30% 工作量)  
3. **Admin 功能新增** (約 10% 工作量)

**關鍵決策已確認**：
- ✅ 倒數計時自動取消機制
- ✅ 直接關聯 chat_rooms.id
- ✅ 特定管理員權限控制
- ✅ 直接狀態更新流程
- ✅ 管理員聊天室查看機制 (直接查看原聊天室，完整歷史，完全透明)
- ✅ 防重複提交機制 (顯示現有爭議資訊，包含 ID、狀態、時間戳記)

## ⚠️ **重要實現前置作業**

### **資料庫結構修正 (必須優先執行)**
在開始任務爭議模組開發前，必須先修正以下資料庫結構問題：

#### **1. 修正 `task_dispute_events` 表結構**
```sql
-- 修正外鍵型別不匹配問題
ALTER TABLE task_dispute_events 
MODIFY COLUMN task_dispute_chat_room_id VARCHAR(128) NOT NULL;

-- 如果 admins.id 已升級為 BIGINT UNSIGNED
ALTER TABLE task_dispute_events 
MODIFY COLUMN admin_id BIGINT UNSIGNED DEFAULT NULL;
```

#### **2. 建立必要索引 (採用方案A)**
```sql
-- 爭議模組必需索引
CREATE INDEX idx_task_dispute_events_task_id ON task_dispute_events(task_id);
CREATE INDEX idx_task_dispute_events_user_id ON task_dispute_events(user_id);
CREATE INDEX idx_task_dispute_events_status ON task_dispute_events(status);
CREATE INDEX idx_task_dispute_events_created_at ON task_dispute_events(created_at);
CREATE UNIQUE INDEX uk_task_dispute_events_chat_room ON task_dispute_events(task_dispute_chat_room_id);

-- 低風險高收益的現有表格索引
CREATE INDEX idx_chat_messages_room_kind_created ON chat_messages(room_id, kind, created_at);
```

#### **3. 驗證外鍵約束**
```sql
-- 檢查所有外鍵是否能正確建立
SHOW CREATE TABLE task_dispute_events;
```

## ✅ **已解決的決策問題**

### **1. 點數轉移 API 整合方式** ✅ **已解決**
**決策**: 直接重用 `confirm_completion.php` 的完整邏輯
- 無需重構，現有邏輯已經非常完善
- 包含手續費計算、點數轉移、餘額更新、交易記錄等完整流程
- 爭議決策 API 中直接調用相同的邏輯代碼

### **2. 系統訊息格式（已決策）**
**問題**: 爭議決策結果的系統訊息應該使用什麼格式？
**決策**:
爭議決策結果的訊息以英文單一語言表示：
```決策訊息（純文字、三行）[chat_rooms.kind = 'system']
Admin Resolution Result: ${`task_dispute_events.decision_result`}
Description:  ${`task_dispute_events.decision_notes`}

Issue Status: ${`task_dispute_events.status`}
Last Update Time:  ${`task_dispute_events.update_at`}
```


### **3. Admin 權限細分**
**問題**: 是否需要更細緻的管理員權限控制？(已回覆)
**考慮因素**:
- 是否需要「只能查看」和「可以決策」的權限區分 (`admin_roles.name` in ('admin', 'super_admin') 擁有操作修改、刪除的權限，其他角色暫時不添加限制)
- 是否需要記錄哪個管理員處理了哪些爭議 （使用： `admin_active_logs` 統一管理後台管理員操作紀錄 )
- 是否需要爭議分配機制 (暫時不用，有權限的管理員操作的時候會需要在 `task_dispute_events.admin_id` 寫入，並且記錄在對應的logs資料表)

### **4. 管理員聊天室查看機制** ✅ **已決策**

#### **4.1 聊天室資料處理方式**
**決策**: 採用方案A - 直接查看原聊天室
- **不需要 Clone**: 管理員直接查看 `task_dispute_events.task_dispute_chat_room_id` 對應的原聊天室
- **完整上下文**: 管理員能看到從任務申請、接受、進行到爭議提交的完整對話過程
- **即時性**: 如果爭議期間還有新訊息，管理員能看到最新狀態
- **節省資源**: 無需額外的資料複製和同步機制

#### **4.2 聊天室類型識別**
**決策**: 採用方案A - `type='application'`
- 保持與原任務聊天室的一致性
- 管理員 API 通過 `task_dispute_events` 關聯查詢，無需改變聊天室類型

#### **4.3 訊息範圍限制**
**決策**: 採用方案A - 完整歷史
- 管理員查看完整聊天記錄（從任務開始到當前）
- 前端可添加爭議提交分界線標記，幫助管理員識別關鍵時間點
- 有助於管理員了解爭議的完整背景和上下文

#### **4.4 用戶隱私保護**
**決策**: 採用方案A - 完全透明
- 管理員可查看所有訊息內容，包括文字、圖片、Resume等
- 管理員需要完整資訊來做出公正的爭議決策
- 符合爭議仲裁的透明度要求

#### **4.5 管理員操作記錄**
**決策**: 採用方案A - 記錄查看行為
- 使用 `admin_active_logs` 記錄管理員查看聊天室的操作
- 有助於審計追蹤和安全監控
- 記錄格式: `action='dispute_chat_viewed'`, `details='dispute_id:{id}'`

## 📋 **可能的資料缺漏**

### **1. 現有 API 端點確認** ✅ **已確認**
**已確認的現有邏輯**:
- ✅ **點數轉移邏輯**: `backend/api/tasks/confirm_completion.php` - 完整的點數轉移、手續費計算、餘額更新
- ✅ **手續費計算**: 從 `task_completion_points_fee_settings` 表讀取費率，使用 `PointTransactionLogger` 記錄
- ✅ **系統訊息**: 直接插入 `chat_messages` 表，`kind='system'`
- ✅ **任務狀態邏輯**: 
  - `tasks.status_id` 使用 `task_statuses.id` (數字)
  - `task_applications.status` 使用字串枚舉
  - 狀態對應關係已確認：
    - `open` (id=1) ↔ `applied`
    - `in_progress` (id=2) ↔ `accepted` 
    - `pending_confirmation` (id=3) ↔ `pending`
    - `dispute` (id=4) ↔ `dispute`
    - `completed` (id=5) ↔ `completed`
- ✅ **倒數計時邏輯**: `backend/cron/auto_complete_tasks.php` - 7天自動完成機制

### **2. 資料庫約束確認** ⚠️ **需要修正**

#### **外鍵約束問題**
根據現有資料庫結構分析，`task_dispute_events` 表的外鍵設定存在型別不一致問題：

**🔴 需要修正的外鍵**:
1. **`task_id`**: 
   - 目前: `varchar(36) NOT NULL`
   - 實際 `tasks.id`: `varchar(36)` (但建議升級為 `BIGINT UNSIGNED`)
   - **建議**: 保持一致性，如果 `tasks.id` 升級則同步升級

2. **`task_dispute_chat_room_id`**:
   - 目前: `bigint NOT NULL`
   - 實際 `chat_rooms.id`: `VARCHAR(128)` (字串格式)
   - **🚨 嚴重問題**: 型別完全不匹配，外鍵無法建立
   - **必須修正**: 改為 `VARCHAR(128) NOT NULL`

3. **`user_id`**:
   - 目前: `bigint UNSIGNED NOT NULL`
   - 實際 `users.id`: `BIGINT UNSIGNED`
   - **✅ 正確**: 無需修改

4. **`admin_id`**:
   - 目前: `int DEFAULT NULL`
   - 實際 `admins.id`: `int` (但建議升級為 `BIGINT UNSIGNED`)
   - **建議**: 如果 `admins.id` 升級則同步升級

#### **修正後的 `task_dispute_events` 表結構**:
```sql
CREATE TABLE task_dispute_events (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  task_id VARCHAR(36) NOT NULL,                    -- 與 tasks.id 一致
  task_dispute_chat_room_id VARCHAR(128) NOT NULL, -- 🔧 修正：與 chat_rooms.id 一致
  user_id BIGINT UNSIGNED NOT NULL,                -- ✅ 正確
  admin_id BIGINT UNSIGNED DEFAULT NULL,           -- 🔧 建議升級
  title VARCHAR(255) NOT NULL,
  description TEXT NOT NULL,
  status ENUM('submitted','in_progress','resolved') DEFAULT 'submitted',
  decision_result ENUM('completed','back_to_progress','reset') DEFAULT NULL,
  decision_note TEXT DEFAULT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  -- 外鍵約束
  FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE,
  FOREIGN KEY (task_dispute_chat_room_id) REFERENCES chat_rooms(id) ON DELETE CASCADE,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (admin_id) REFERENCES admins(id) ON DELETE SET NULL
);
```

#### **索引優化建議 （已執行成功）**:
```sql
-- 基本索引  (✅已執行）
CREATE INDEX idx_task_dispute_events_task_id ON task_dispute_events(task_id);
CREATE INDEX idx_task_dispute_events_user_id ON task_dispute_events(user_id);
CREATE INDEX idx_task_dispute_events_admin_id ON task_dispute_events(admin_id);
CREATE INDEX idx_task_dispute_events_status ON task_dispute_events(status);
CREATE INDEX idx_task_dispute_events_created_at ON task_dispute_events(created_at);

-- 複合索引（查詢優化） (✅已執行）
CREATE INDEX idx_task_dispute_events_status_created ON task_dispute_events(status, created_at);
CREATE INDEX idx_task_dispute_events_user_status ON task_dispute_events(user_id, status);

-- 唯一約束（防止重複爭議） (✅已執行）
CREATE UNIQUE INDEX uk_task_dispute_events_chat_room ON task_dispute_events(task_dispute_chat_room_id);
```

#### **相關表格索引優化**:
```sql
-- task_dispute_event_logs 表 (✅已執行）
CREATE INDEX idx_task_dispute_event_logs_event_id ON task_dispute_event_logs(event_id);
CREATE INDEX idx_task_dispute_event_logs_admin_id ON task_dispute_event_logs(admin_id);
CREATE INDEX idx_task_dispute_event_logs_created_at ON task_dispute_event_logs(created_at);

-- 現有表格優化 (⚠️ 需評估影響)
CREATE INDEX idx_tasks_status_updated ON tasks(status_id, updated_at);
CREATE INDEX idx_task_applications_user_status ON task_applications(user_id, status);
CREATE INDEX idx_chat_messages_room_kind_created ON chat_messages(room_id, kind, created_at);
```

### **3. 前端路由配置** ✅ **已確認**
**已確認需求**:
- ✅ **Admin 後台導航**: 需要新增爭議管理入口到導航選單
- ✅ **任務列表整合**: 在現有任務列表中顯示爭議狀態，爭議狀態可點擊前往爭議管理頁面
- ✅ **Flutter App**: 暫時不做爭議歷史查看功能

**具體實現需求**:
1. **Admin 導航選單新增**: 
   - 路由: `/task-disputes`
   - 標題: "Task Disputes"
   - 圖示: `Icons.gavel` 或 `Icons.report_problem`

2. **任務列表狀態欄位**:
   - 當 `tasks.status_id = 4` (dispute) 時，狀態欄位顯示為可點擊的連結
   - 點擊後導向: `/task-disputes?task_id={task_id}`

3. **Flutter App Action Bar**:
   - 爭議狀態下的 'dispute' 按鈕保持現有功能
   - 不新增額外的歷史查看功能

## 📊 **索引優化策略總結**

### **🎯 索引執行策略**

#### **✅ 採用方案 A (保守執行)**
已在前置作業中採用，包含：
- 爭議模組必需的 5 個索引
- 低風險高收益的 chat_messages 索引

#### **📋 方案 B 備註 (可選執行)**
如需進一步效能優化，可考慮執行以下額外索引：

```sql
-- 方案 B: 額外的現有表格索引優化
-- ⚠️ 需要監控執行，建議在方案 A 穩定運行後考慮

-- tasks 表索引 (影響程度: 中等)
CREATE INDEX idx_tasks_status_updated ON tasks(status_id, updated_at);
-- 預期效果: 任務列表查詢提升 30-50%
-- 影響 API: tasks/list.php, tasks/history.php, posted_task_applications.php 等

-- task_applications 表索引 (影響程度: 中等)  
CREATE INDEX idx_task_applications_user_status ON task_applications(user_id, status);
-- 預期效果: 應徵記錄查詢提升 40-60%
-- 影響 API: my_work_applications.php, task_applicants.php 等
```

**方案 B 執行條件**:
- 方案 A 運行穩定 1-2 週後
- 基於實際效能監控數據決策
- 需要準備回滾方案

**監控指標**:
- 任務列表 API 回應時間 (目標: <200ms)
- 應徵提交成功率 (維持: >99%)
- 資料庫 CPU 使用率 (警戒: >80%)

### **📋 實現總結**

**✅ 已確定執行方案**:
- 採用方案 A (保守執行)
- 包含爭議模組必需索引 + chat_messages 優化
- 方案 B 已備註供後續參考

**🎯 下一步行動**:
1. 執行資料庫結構修正
2. 建立必要索引 (方案 A)
3. 開始任務爭議模組開發
4. 監控系統效能表現

